# TypeScript Mirror (Optional)

This folder mirrors the AVOT chassis contract for TS environments.
It is optional; the canonical contract is in `contracts/AVOT_Interface.md`.

If you want to run TS examples later, initialize a Node project here.
